name: "Collect osm apps action"

inputs:
  ghToken:
    description: "GitHub access token"
    required: true

  goatcounterToken:
    description: "GoatCounter api token to read stats"
    required: true

runs:
  using: node24
  main: ../../dist/actions/collect-osm-apps/index.js
